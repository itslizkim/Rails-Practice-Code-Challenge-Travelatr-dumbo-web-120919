<div>
    <h1> <%= @post.title %></h1>
    <h2> <%= link_to @blogger.name, blogger_path(@blogger.id)%> </h2>
    <h2> <%= link_to @destination.name, destination_path(@destination.id) %> </h2>
    <h4> <%= @post.content %> </h4>
    <h4> Likes 💓<%= @post.likes %> </h4>
    

    <span><%= button_to "Like 💓", like_path(@post.id)%></span>


</div>